{
  "name": "flight-currency",
  "version": "1.1.3",
  "private": true,
  "description": "A minimal currency preference for Google Flights.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "package": "python3 scripts/package.py"
  }
}
